# Core Dependencies
streamlit==1.29.0
anthropic==0.8.0
python-dotenv==1.0.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
Pillow==12.1.0  # pinned to avoid Windows build issues; see INSTALLATION_GUIDE.md
# PDF Generation (for future enhancement)
reportlab==4.0.7
markdown==3.5.1

# Data Handling
# Allow newer pandas for compatibility with newer Python (e.g., 3.14)
# Use a specific tested wheel where possible
pandas==2.3.3  # updated to support Python 3.14 and newer wheels

# Utilities
requests==2.31.0
